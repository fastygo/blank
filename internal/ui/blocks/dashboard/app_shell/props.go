package appshell

import "github.com/fastygo/blank/internal/ui/layout"

// Props configures the topnav app shell registry artifact (document chrome + main slot).
type Props struct {
	Shell layout.ShellProps
}
