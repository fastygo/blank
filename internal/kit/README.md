# Kit — local atoms (codegen)

Stand-in for future `github.com/fastygo/templ` (`ui/` + `utils/`).

| Path | Role |
|------|------|
| `ui/*` | UI8Kit codegen bricks — **do not hand-edit** |
| `utils/` | `Compose`, CVA parse, tag allow-lists, ARIA helpers |

Regenerate from ui8kit-codegen with module path `github.com/fastygo/blank/internal/kit`.
Registry composition lives in [`../ui`](../ui).
