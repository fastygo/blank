// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Label associates visible text with a form control via the for attribute.
*/

package label

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// LabelProps configures Label.
type LabelProps struct {
	// Class — adds Tailwind utilities
	Class string
	// HTMLFor — id of the labeled control
	HTMLFor string
	Attrs templ.Attributes
}

// LabelClasses composes Label classes without rendering —
// the server-composition escape hatch for custom root elements.
func LabelClasses(p LabelProps) string {
	return utils.Compose(LabelVariants, map[string]string{}, p.Class)
}

// Label renders the Label contract.
templ Label(p LabelProps) {
	<label
		class={ LabelClasses(p) }
		if utils.IsSetStr(p.HTMLFor) {
			for={ p.HTMLFor }
		}
		{ p.Attrs... }
	>
		{ children... }
	</label>
}
