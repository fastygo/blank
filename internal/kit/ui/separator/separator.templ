// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Separator renders a semantic or decorative hr.
*/

package separator

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// SeparatorProps configures Separator.
type SeparatorProps struct {
	// Variant — sets appearance preset
	Variant string
	// Orientation — horizontal or vertical
	Orientation string
	// Class — adds Tailwind utilities
	Class string
	// Decorative — hides the rule from assistive technology
	Decorative bool
	Attrs templ.Attributes
}

// SeparatorClasses composes Separator classes without rendering —
// the server-composition escape hatch for custom root elements.
func SeparatorClasses(p SeparatorProps) string {
	return utils.Compose(SeparatorVariants, map[string]string{
		"variant": p.Variant,
		"orientation": p.Orientation,
	}, p.Class)
}

// Separator renders the Separator contract.
templ Separator(p SeparatorProps) {
	<hr
		class={ SeparatorClasses(p) }
		if p.Decorative {
			role={ "presentation" }
		}
		if p.Decorative {
			aria-hidden={ "true" }
		}
		if p.Orientation == "vertical" {
			aria-orientation={ "vertical" }
		}
		{ p.Attrs... }
	/>
}
