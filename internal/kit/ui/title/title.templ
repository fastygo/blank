// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Title renders a heading (h1–h6) from As (1–6). Defaults to h2 when As is 0
or out of range.
*/

package title

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// TitleProps configures Title.
type TitleProps struct {
	// Class — adds Tailwind utilities
	Class string
	// As — maps to h1–h6; 0 and 2 default to h2
	As utils.HeadingLevel
	Attrs templ.Attributes
}

// TitleClasses composes Title classes without rendering —
// the server-composition escape hatch for custom root elements.
func TitleClasses(p TitleProps) string {
	return utils.Compose(TitleVariants, map[string]string{}, p.Class)
}

// Title renders the Title contract.
templ Title(p TitleProps) {
	switch utils.TitleTag(p.As) {
		case "h1":
			<h1
				class={ TitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h1>
		case "h3":
			<h3
				class={ TitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h3>
		case "h4":
			<h4
				class={ TitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h4>
		case "h5":
			<h5
				class={ TitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h5>
		case "h6":
			<h6
				class={ TitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h6>
		default:
			<h2
				class={ TitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h2>
	}
}
