// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Break renders br or wbr for controlled inline wrapping.
*/

package linebreak

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// BreakProps configures Break.
type BreakProps struct {
	// Type — br (default) or wbr/word
	Type string
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// BreakClasses composes Break classes without rendering —
// the server-composition escape hatch for custom root elements.
func BreakClasses(p BreakProps) string {
	return utils.Compose(LinebreakVariants, map[string]string{}, p.Class)
}

// Break renders the Break contract.
templ Break(p BreakProps) {
	switch utils.MapOr(p.Type, map[string]string{"wbr": "wbr", "word": "wbr"}, "br") {
		case "wbr":
			<wbr class={ BreakClasses(p) } { p.Attrs... }/>
		default:
			<br class={ BreakClasses(p) } { p.Attrs... }/>
	}
}
