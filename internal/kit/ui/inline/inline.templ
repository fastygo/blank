// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Inline renders inline copy as a span. Use Text for paragraph blocks and
Title (as 1–6) for headings.
*/

package inline

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// InlineProps configures Inline.
type InlineProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// InlineClasses composes Inline classes without rendering —
// the server-composition escape hatch for custom root elements.
func InlineClasses(p InlineProps) string {
	return utils.Compose(InlineVariants, map[string]string{}, p.Class)
}

// Inline renders the Inline contract.
templ Inline(p InlineProps) {
	<span
		class={ InlineClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</span>
}
