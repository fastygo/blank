// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Text renders a paragraph of body copy. Use Inline for inline span copy
and Title (as 1–6) for headings.
*/

package text

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// TextProps configures Text.
type TextProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// TextClasses composes Text classes without rendering —
// the server-composition escape hatch for custom root elements.
func TextClasses(p TextProps) string {
	return utils.Compose(TextVariants, map[string]string{}, p.Class)
}

// Text renders the Text contract.
templ Text(p TextProps) {
	<p
		class={ TextClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</p>
}
