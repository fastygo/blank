// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Radio is a native single-choice control within a named group.
*/

package radio

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// RadioProps configures Radio.
type RadioProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	Name string
	Value string
	Checked bool
	Disabled bool
	Required bool
	// ID — element id
	ID string
	// Role — ARIA role override
	Role string
	// TabIndex — tab order override
	TabIndex string
	// AriaLabel — accessible name for unlabeled controls
	AriaLabel string
	Attrs templ.Attributes
}

// RadioClasses composes Radio classes without rendering —
// the server-composition escape hatch for custom root elements.
func RadioClasses(p RadioProps) string {
	return utils.Compose(RadioVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Radio renders the Radio contract.
templ Radio(p RadioProps) {
	<input
		if utils.IsSetStr(p.ID) {
			id={ p.ID }
		}
		type="radio"
		if utils.IsSetStr(p.Name) {
			name={ p.Name }
		}
		if utils.IsSetStr(p.Value) {
			value={ p.Value }
		}
		class={ RadioClasses(p) }
		checked?={ p.Checked }
		disabled?={ p.Disabled }
		required?={ p.Required }
		if utils.IsSetStr(p.Role) {
			role={ p.Role }
		}
		if utils.IsSetStr(p.TabIndex) {
			tabindex={ p.TabIndex }
		}
		if utils.IsSetStr(p.AriaLabel) {
			aria-label={ p.AriaLabel }
		}
		{ p.Attrs... }
	/>
}
