// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Container wraps page content with a constrained width shell.
Optional main or section tag for inner layout regions.
*/

package container

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// ContainerProps configures Container.
type ContainerProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Tag — div, main, or section
	Tag string
	Attrs templ.Attributes
}

// ContainerClasses composes Container classes without rendering —
// the server-composition escape hatch for custom root elements.
func ContainerClasses(p ContainerProps) string {
	return utils.Compose(ContainerVariants, map[string]string{}, p.Class)
}

// Container renders the Container contract.
templ Container(p ContainerProps) {
	switch utils.ResolveTag(p.Tag, "div", utils.TagGroupContainer) {
		case "main":
			<main
				class={ ContainerClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</main>
		case "section":
			<section
				class={ ContainerClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</section>
		default:
			<div
				class={ ContainerClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</div>
	}
}
