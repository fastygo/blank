// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Switch is a toggle control (checkbox with switch ARIA semantics).
Use for on/off settings; wireframe uses native checkbox styling.
*/

package formswitch

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// SwitchProps configures Switch.
type SwitchProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	Name string
	Value string
	Checked bool
	Disabled bool
	Required bool
	ID string
	TabIndex string
	AriaLabel string
	Attrs templ.Attributes
}

// SwitchClasses composes Switch classes without rendering —
// the server-composition escape hatch for custom root elements.
func SwitchClasses(p SwitchProps) string {
	return utils.Compose(SwitchVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Switch renders the Switch contract.
templ Switch(p SwitchProps) {
	<input
		if utils.IsSetStr(p.ID) {
			id={ p.ID }
		}
		type="checkbox"
		if utils.IsSetStr(p.Name) {
			name={ p.Name }
		}
		if utils.IsSetStr(p.Value) {
			value={ p.Value }
		}
		class={ SwitchClasses(p) }
		checked?={ p.Checked }
		disabled?={ p.Disabled }
		required?={ p.Required }
		if utils.IsSetStr(p.TabIndex) {
			tabindex={ p.TabIndex }
		}
		if utils.IsSetStr(p.AriaLabel) {
			aria-label={ p.AriaLabel }
		}
		role="switch"
		aria-checked={ utils.BoolStr(p.Checked) }
		{ p.Attrs... }
	/>
}
