// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Checkbox is a native boolean form control.
Wireframe styling via Variant and Size; supports standard ARIA attrs.
*/

package checkbox

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// CheckboxProps configures Checkbox.
type CheckboxProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	Name string
	Value string
	Checked bool
	Disabled bool
	Required bool
	// ID — element id
	ID string
	// Role — ARIA role override
	Role string
	// TabIndex — tab order override
	TabIndex string
	// AriaLabel — accessible name for unlabeled controls
	AriaLabel string
	Attrs templ.Attributes
}

// CheckboxClasses composes Checkbox classes without rendering —
// the server-composition escape hatch for custom root elements.
func CheckboxClasses(p CheckboxProps) string {
	return utils.Compose(CheckboxVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Checkbox renders the Checkbox contract.
templ Checkbox(p CheckboxProps) {
	<input
		if utils.IsSetStr(p.ID) {
			id={ p.ID }
		}
		type="checkbox"
		if utils.IsSetStr(p.Name) {
			name={ p.Name }
		}
		if utils.IsSetStr(p.Value) {
			value={ p.Value }
		}
		class={ CheckboxClasses(p) }
		checked?={ p.Checked }
		disabled?={ p.Disabled }
		required?={ p.Required }
		if utils.IsSetStr(p.Role) {
			role={ p.Role }
		}
		if utils.IsSetStr(p.TabIndex) {
			tabindex={ p.TabIndex }
		}
		if utils.IsSetStr(p.AriaLabel) {
			aria-label={ p.AriaLabel }
		}
		{ p.Attrs... }
	/>
}
