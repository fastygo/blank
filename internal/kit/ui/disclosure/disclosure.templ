// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Disclosure renders native details and summary elements.
Open is a static attribute only — no controlled-state contract.
*/

package disclosure

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// DisclosureProps configures Disclosure.
type DisclosureProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	// Open — initial open state (static attribute only)
	Open bool
	Attrs templ.Attributes
}

// DisclosureClasses composes Disclosure classes without rendering —
// the server-composition escape hatch for custom root elements.
func DisclosureClasses(p DisclosureProps) string {
	return utils.Compose(DisclosureVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Disclosure renders the Disclosure contract.
templ Disclosure(p DisclosureProps) {
	<details
		class={ DisclosureClasses(p) }
		open?={ p.Open }
		{ p.Attrs... }
	>
		{ children... }
	</details>
}

// SummaryProps configures Summary.
type SummaryProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// SummaryClasses composes Summary classes without rendering —
// the server-composition escape hatch for custom root elements.
func SummaryClasses(p SummaryProps) string {
	return utils.Compose(SummaryVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Summary renders the Summary contract.
templ Summary(p SummaryProps) {
	<summary
		class={ SummaryClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</summary>
}
