// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Dialog renders a native dialog root with an optional behavior hook.
The data-ui8kit hook defaults off; the registry ships no client JS.
*/

package dialog

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// DialogProps configures Dialog.
type DialogProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	ID string
	// Open — initial open state (static attribute only)
	Open bool
	// DataUI8Kit — opt-in behavior hook; empty emits nothing
	DataUI8Kit string
	AriaLabel string
	AriaLabelledBy string
	AriaDescribedBy string
	Attrs templ.Attributes
}

// DialogClasses composes Dialog classes without rendering —
// the server-composition escape hatch for custom root elements.
func DialogClasses(p DialogProps) string {
	return utils.Compose(DialogVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Dialog renders the Dialog contract.
templ Dialog(p DialogProps) {
	<dialog
		class={ DialogClasses(p) }
		open?={ p.Open }
		if utils.IsSetStr(p.ID) {
			id={ p.ID }
		}
		if utils.IsSetStr(p.DataUI8Kit) {
			data-ui8kit={ p.DataUI8Kit }
		}
		if utils.IsSetStr(p.AriaLabel) {
			aria-label={ p.AriaLabel }
		}
		if utils.IsSetStr(p.AriaLabelledBy) {
			aria-labelledby={ p.AriaLabelledBy }
		}
		if utils.IsSetStr(p.AriaDescribedBy) {
			aria-describedby={ p.AriaDescribedBy }
		}
		{ p.Attrs... }
	>
		{ children... }
	</dialog>
}
