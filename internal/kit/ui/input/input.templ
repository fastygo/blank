// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Input is a single-line native text field.
Supports types, validation attrs, and wireframe variant/size classes.
*/

package input

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// InputProps configures Input.
type InputProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	// Type — input type; defaults to text
	Type string
	Name string
	Value string
	Placeholder string
	Disabled bool
	Required bool
	Autocomplete string
	Min string
	Max string
	// ID — element id
	ID string
	// Role — ARIA role override
	Role string
	// TabIndex — tab order override
	TabIndex string
	// AriaLabel — accessible name for unlabeled controls
	AriaLabel string
	Attrs templ.Attributes
}

// InputClasses composes Input classes without rendering —
// the server-composition escape hatch for custom root elements.
func InputClasses(p InputProps) string {
	return utils.Compose(InputVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Input renders the Input contract.
templ Input(p InputProps) {
	<input
		if utils.IsSetStr(p.ID) {
			id={ p.ID }
		}
		type={ utils.DefaultIfEmpty(p.Type, "text") }
		if utils.IsSetStr(p.Name) {
			name={ p.Name }
		}
		if utils.IsSetStr(p.Value) {
			value={ p.Value }
		}
		if utils.IsSetStr(p.Placeholder) {
			placeholder={ p.Placeholder }
		}
		if utils.IsSetStr(p.Autocomplete) {
			autocomplete={ p.Autocomplete }
		}
		if utils.IsSetStr(p.Min) {
			min={ p.Min }
		}
		if utils.IsSetStr(p.Max) {
			max={ p.Max }
		}
		class={ InputClasses(p) }
		disabled?={ p.Disabled }
		required?={ p.Required }
		if utils.IsSetStr(p.Role) {
			role={ p.Role }
		}
		if utils.IsSetStr(p.TabIndex) {
			tabindex={ p.TabIndex }
		}
		if utils.IsSetStr(p.AriaLabel) {
			aria-label={ p.AriaLabel }
		}
		{ p.Attrs... }
	/>
}
