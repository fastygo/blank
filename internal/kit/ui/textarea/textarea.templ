// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Textarea is a multi-line native text field with row sizing.
*/

package textarea

import (
	"strconv"

	utils "github.com/fastygo/blank/internal/kit/utils"
)

// TextareaProps configures Textarea.
type TextareaProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	Name string
	// Value — initial textarea content
	Value string
	Placeholder string
	// Rows — visible line count; defaults to 4 when zero
	Rows int
	Disabled bool
	Required bool
	// ID — element id
	ID string
	// Role — ARIA role override
	Role string
	// TabIndex — tab order override
	TabIndex string
	// AriaLabel — accessible name for unlabeled controls
	AriaLabel string
	Attrs templ.Attributes
}

// TextareaClasses composes Textarea classes without rendering —
// the server-composition escape hatch for custom root elements.
func TextareaClasses(p TextareaProps) string {
	return utils.Compose(TextareaVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Textarea renders the Textarea contract.
templ Textarea(p TextareaProps) {
	<textarea
		if utils.IsSetStr(p.ID) {
			id={ p.ID }
		}
		if utils.IsSetStr(p.Name) {
			name={ p.Name }
		}
		class={ TextareaClasses(p) }
		rows={ utils.If((int(p.Rows) > 0), strconv.Itoa(int(p.Rows)), "4") }
		if utils.IsSetStr(p.Placeholder) {
			placeholder={ p.Placeholder }
		}
		disabled?={ p.Disabled }
		required?={ p.Required }
		if utils.IsSetStr(p.Role) {
			role={ p.Role }
		}
		if utils.IsSetStr(p.TabIndex) {
			tabindex={ p.TabIndex }
		}
		if utils.IsSetStr(p.AriaLabel) {
			aria-label={ p.AriaLabel }
		}
		{ p.Attrs... }
	>
		{ p.Value }
	</textarea>
}
