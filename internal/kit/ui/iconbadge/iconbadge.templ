// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
IconBadge renders a small icon surface for nav rows, cards, and status blocks.
Pass the letter, glyph, or Icon node as children.
*/

package iconbadge

import (
	utils "github.com/fastygo/blank/internal/kit/utils"
)

// IconBadgeProps configures IconBadge.
type IconBadgeProps struct {
	// Variant — sets appearance preset
	Variant string
	// Size — sets density preset
	Size string
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// IconBadgeClasses composes IconBadge classes without rendering —
// the server-composition escape hatch for custom root elements.
func IconBadgeClasses(p IconBadgeProps) string {
	return utils.Compose(IconbadgeVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// IconBadge renders the IconBadge contract.
templ IconBadge(p IconBadgeProps) {
	<span
		class={ IconBadgeClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</span>
}
